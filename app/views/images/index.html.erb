<h1>photo market inc</h1>

<div id="images">
  <% if @images.any? %>
    <% @images.each do |image| %>
      <%= image.name %>
      <%= image_tag image.file.url %>
    <% end %>
  <% else %>
    <p>No images yet!</p>
  <% end %>

</div>
